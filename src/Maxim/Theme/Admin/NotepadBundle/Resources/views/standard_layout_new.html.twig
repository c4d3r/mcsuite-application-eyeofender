{% extends 'SonataAdminBundle::standard_layout.html.twig' %}

{% block stylesheets %}


    <link rel="stylesheet" href="{{ asset('bundles/maximthemeadminnotepad/css/bootstrap.css') }}" type="text/css" media="all" />
    <link rel="stylesheet" href="{{ asset('bundles/maximthemeadminnotepad/css/animate.css') }}" type="text/css" media="all" />
    <link rel="stylesheet" href="{{ asset('bundles/maximthemeadminnotepad/css/font-awesome.min.css') }}" type="text/css" media="all" />
    <link rel="stylesheet" href="{{ asset('bundles/maximthemeadminnotepad/css/font.css') }}" type="text/css" media="all" />
    <link rel="stylesheet" href="{{ asset('bundles/maximthemeadminnotepad/css/maxim-extra.css') }}" type="text/css" media="all" />
    <link rel="stylesheet" href="{{ asset('bundles/maximthemeadminnotepad/js/calendar/bootstrap_calendar.css') }}" type="text/css" media="all" />


    <style>
        .select2-container {
            padding:0 !important;
            border:none !important;
            width: auto;
            min-width: 65px;
            margin-right:5px;
        }
        .per-page {
            min-width:55px !important;
        }
        .select2-container + .select2-container {

        }
        #filter_date_value {
            padding:0;
            margin-bottom: 40px;
            border: none;
        }
        .select2-container .select2-choice {
            border-color: #d9d9d9 !important;
            border-radius: 2px !important;
            height: 34px !important;
            line-height:34px !important;
        }

        .select2-container .select2-choice .select2-arrow {
            border-radius: 0 !important;
        }
    </style>

    {% if admin_pool is defined and admin_pool.getOption('use_select2') %}
        {#<link rel="stylesheet" href="{{ asset('bundles/maximthemeadminnotepad/css/select2.css') }}" type="text/css" media="all" />#}
        <link rel="stylesheet" href="{{ asset('bundles/maximthemeadminnotepad/js/select2/select2.css') }}" type="text/css" media="all" />
        <link rel="stylesheet" href="{{ asset('bundles/maximthemeadminnotepad/js/select2/theme.css') }}" type="text/css" media="all" />
    {% endif %}

    <link rel="stylesheet" href="{{ asset('bundles/maximcms/plugins/redactor/redactor.css') }}" type="text/css" media="all" />
    <link rel="stylesheet" href="{{ asset('bundles/maximthemeadminnotepad/css/app.css') }}" type="text/css" media="all" />
{% endblock %}

{% block javascripts %}

    <script src="//code.jquery.com/jquery-1.11.0.min.js"></script>
    {#<script src="//maxcdn.bootstrapcdn.com/bootstrap/3.2.0/js/bootstrap.min.js"></script>#}

    {{ parent() }}

    <!--[if lt IE 9]>
    <script src="{{ asset('bundles/maximthemeadminnotepad/js/ie/html5shiv.js') }}" cache="false"></script>
    <script src="{{ asset('bundles/maximthemeadminnotepad/js/ie/respond.min.js') }}" cache="false"></script>
    <script src="{{ asset('bundles/maximthemeadminnotepad/js/ie/excanvas.js') }}" cache="false"></script>
    <![endif]-->

    <script src="{{ asset('bundles/maximthemeadminnotepad/js/bootstrap.js') }}"></script>

    <script src="{{ asset('bundles/maximcms/js/admin/resendPurchase.js') }}" type="text/javascript"></script>

    <!-- App -->
    <script src="{{ asset('bundles/maximthemeadminnotepad/js/app.js') }}"></script>
    <script src="{{ asset('bundles/maximthemeadminnotepad/js/app.plugin.js') }}"></script>
    <script src="{{ asset('bundles/maximthemeadminnotepad/js/slimscroll/jquery.slimscroll.min.js') }}"></script>
    <script src="{{ asset('bundles/maximthemeadminnotepad/js/charts/easypiechart/jquery.easy-pie-chart.js') }}" cache="false"></script>
    <script src="{{ asset('bundles/maximthemeadminnotepad/js/charts/sparkline/jquery.sparkline.min.js') }}" cache="false"></script>
    <script src="{{ asset('bundles/maximthemeadminnotepad/js/charts/flot/jquery.flot.min.js') }}" cache="false"></script>
    <script src="{{ asset('bundles/maximthemeadminnotepad/js/charts/flot/jquery.flot.tooltip.min.js') }}" cache="false"></script>
    <script src="{{ asset('bundles/maximthemeadminnotepad/js/charts/flot/jquery.flot.resize.js') }}" cache="false"></script>
    <script src="{{ asset('bundles/maximthemeadminnotepad/js/charts/flot/jquery.flot.grow.js') }}" cache="false"></script>
    <script src="{{ asset('bundles/maximthemeadminnotepad/js/charts/flot/jquery.flot.time.js') }}" cache="false"></script>
    <script src="{{ asset('bundles/maximthemeadminnotepad/js/calendar/bootstrap_calendar.js') }}" cache="false"></script>

    <script src="{{ asset('bundles/maximcms/plugins/redactor/redactor.min.js') }}"></script>
    <script src="{{ asset('bundles/maximcms/js/redactor_init.js') }}"></script>

    <script src="{{ asset('bundles/maximthemeadminnotepad/js/sortable/jquery.sortable.js') }}" cache="false"></script>

    <script>
        $(function(){
            $('input').each(function(){
                var inputAttr = $(this).attr("type")
                if($.inArray(inputAttr, ["checkbox", "submit"]) == -1) {
                    $(this).addClass("form-control");
                }
            });
        })
    </script>

{% endblock %}

{% block body_attributes %}{% endblock %}

{% block sonata_header %}
<section class="vbox">
    <header class="bg-dark dk header navbar navbar-fixed-top-xs">
        <div class="navbar-header aside-md">
            <a class="btn btn-link visible-xs" data-toggle="class:nav-off-screen" data-target="#nav">
                <i class="fa fa-bars"></i>
            </a>
            <a href="#" class="navbar-brand" data-toggle="fullscreen">{{ admin_pool.title }}</a>
            <a class="btn btn-link visible-xs" data-toggle="dropdown" data-target=".nav-user">
                <i class="fa fa-cog"></i>
            </a>
        </div>
        <ul class="nav navbar-nav hidden-xs">
            <li class="dropdown">
                <a href="#" class="dropdown-toggle dker" data-toggle="dropdown">
                    <i class="fa fa-building-o"></i>
                    <span class="font-bold">Activity</span>
                </a>
                <section class="dropdown-menu aside-xl on animated fadeInLeft no-borders lt">
                    <div class="wrapper lter m-t-n-xs">
                        <a href="#" class="thumb pull-left m-r">
                            <img src="http://minotar.net/helm/{{ app.user.username }}" class="img-circle">
                        </a>
                        <div class="clear">
                            <a href="#"><span class="text-white font-bold">{{ app.user.username }}</span></a>
                            <small class="block">{{ app.user.group.name }}</small>
                            <a href="#" class="btn btn-xs btn-success m-t-xs"></a>
                        </div>
                    </div>
                    <div class="row m-l-none m-r-none m-b-n-xs text-center">
                        <div class="col-xs-4">
                            <div class="padder-v">
                                <span class="m-b-xs h4 block text-white">999</span>
                                <small class="text-muted">placeholder</small>
                            </div>
                        </div>
                        <div class="col-xs-4 dk">
                            <div class="padder-v">
                                <span class="m-b-xs h4 block text-white">999</span>
                                <small class="text-muted">placeholder</small>
                            </div>
                        </div>
                        <div class="col-xs-4">
                            <div class="padder-v">
                                <span class="m-b-xs h4 block text-white">999</span>
                                <small class="text-muted">placeholder</small>
                            </div>
                        </div>
                    </div>
                </section>
            </li>
        </ul>
        <ul class="nav navbar-nav navbar-right hidden-xs nav-user">
            <li class="dropdown hidden-xs">
                <a href="#" class="dropdown-toggle dker" data-toggle="dropdown"><i class="fa fa-fw fa-search"></i></a>
                <section class="dropdown-menu aside-xl animated fadeInUp">
                    <section class="panel bg-white">
                        <form role="search">
                            <div class="form-group wrapper m-b-none">
                                <div class="input-group">
                                    <input type="text" class="form-control" placeholder="Search">
                            <span class="input-group-btn">
                              <button type="submit" class="btn btn-info btn-icon"><i class="fa fa-search"></i></button>
                            </span>
                                </div>
                            </div>
                        </form>
                    </section>
                </section>
            </li>
            <li class="dropdown">
                <a href="#" class="dropdown-toggle" data-toggle="dropdown">
                    <span class="thumb-sm avatar pull-left">
                      <img src="http://minotar.net/helm/{{ app.user.username }}" alt="{{ app.user.username }}">
                    </span>
                    {{ app.user.username }}<b class="caret"></b>
                </a>
                <ul class="dropdown-menu animated fadeInRight">
                    <span class="arrow top"></span>
                    <li>
                        <a href="{{ path('home') }}">Home</a>
                    </li>
                    <li>
                        <a href="{{ path('account') }}">Settings</a>
                    </li>
                    {% if is_granted('ROLE_PREVIOUS_ADMIN') %}
                        <li>
                            <a href="?_switch_user=_exit" title="stop impersonate user">Stop impersonating</a>
                        </li>
                    {% endif %}
                    {#<li>
                        <a href="#">
                            <span class="badge bg-danger pull-right">3</span>
                            Notifications
                        </a>
                    </li>#}
                    <li class="divider"></li>
                    <li>
                        <a href="{{ path('logout') }}" data-toggle="ajaxModal" >Logout</a>
                    </li>
                </ul>
            </li>
        </ul>
    </header>
{% endblock %}


{% block sonata_wrapper %}
    <section>
        <section class="hbox stretch">
            <!-- .aside -->
            <aside class="bg-dark lter aside-md hidden-print" id="nav">
                <section class="vbox">
                    <header class="header bg-primary lter text-center clearfix">
                        <div class="btn-group">
                            <button type="button" class="btn btn-sm btn-dark btn-icon" title="New project"><i class="fa fa-plus"></i></button>
                            <div class="btn-group hidden-nav-xs">
                                <button type="button" class="btn btn-sm btn-primary dropdown-toggle" data-toggle="dropdown">
                                    Switch Project
                                    <span class="caret"></span>
                                </button>
                                <ul class="dropdown-menu text-left">
                                    <li><a href="#">MCSuite</a></li>
                                </ul>
                            </div>
                        </div>
                    </header>
                    <section class="w-f scrollable">
                        <div class="slim-scroll" data-height="auto" data-disable-fade-out="true" data-distance="0" data-size="5px" data-color="#333333">

                            <!-- nav -->
                            {% if admin_pool is defined %}
                                {% block sonata_nav_menu %}

                                    <nav class="nav-primary hidden-xs">
                                        <ul class="nav">
                                            {% block top_bar_before_nav %} {% endblock %}
                                            {% block sonata_top_bar_nav %}
                                                {% if app.security.token and is_granted('ROLE_SONATA_ADMIN') %}
                                                    {% for group in admin_pool.dashboardgroups %}
                                                        {% set display = (group.roles is empty or is_granted('ROLE_SUPER_ADMIN') ) %}
                                                        {% for role in group.roles if not display %}
                                                            {% set display = is_granted(role) %}
                                                        {% endfor %}

                                                        {# Do not display the group label if no item in group is available #}
                                                        {% set item_count = 0 %}
                                                        {% if display %}
                                                            {% for admin in group.items if item_count == 0 %}
                                                                {% if admin.hasroute('list') and admin.isGranted('LIST') %}
                                                                    {% set item_count = item_count+1 %}
                                                                {% endif %}
                                                            {% endfor %}
                                                        {% endif %}

                                                        {% if display and (item_count > 0) %}
                                                            <li>
                                                                <a href="#">
                                                                    <i class="fa {{ random(['fa-flask', 'fa-envelope', 'fa-columns', 'fa-dashboard', 'fa-pencil']) }} icon">
                                                                        <b class="{{ random(['bg-success', 'bg-warning', 'bg-danger', 'bg-info', 'bg-primary']) }}"></b>
                                                                    </i>
                                                                            <span class="pull-right">
                                                                                <i class="fa fa-angle-down text"></i>
                                                                                <i class="fa fa-angle-up text-active"></i>
                                                                            </span>

                                                                    <span>{{ group.label|trans({}, group.label_catalogue) }}</span>
                                                                </a>
                                                                <ul class="nav lt">
                                                                    {% for admin in group.items %}
                                                                        {% if admin.hasroute('list') and admin.isGranted('LIST') %}
                                                                            <li>
                                                                                <a href="{{ admin.generateUrl('list')}}">
                                                                                    <i class="fa fa-angle-right"></i>
                                                                                    <span>{{ admin.label|trans({}, admin.translationdomain) }}</span>
                                                                                </a>
                                                                            </li>
                                                                        {% endif %}
                                                                    {% endfor %}
                                                                </ul>
                                                            </li>
                                                        {% endif %}
                                                    {% endfor %}
                                                {% endif %}
                                            {% endblock %}
                                            {% block top_bar_after_nav %} {% endblock %}
                                        </ul>
                                    </nav>
                                    <!-- / nav -->
                                {% endblock %}

                                {% block sonata_nav_menu_after %}{% endblock %}
                            {% endif %}
                        </div>
                    </section>
                    <footer class="footer lt hidden-xs b-t b-dark">
                        <div id="chat" class="dropup">
                            <section class="dropdown-menu on aside-md m-l-n">
                                <section class="panel bg-white">
                                    <header class="panel-heading b-b b-light">Active chats</header>
                                    <div class="panel-body animated fadeInRight">
                                        <p class="text-sm">No active chats.</p>
                                        <p><a href="#" class="btn btn-sm btn-default">Start a chat</a></p>
                                    </div>
                                </section>
                            </section>
                        </div>
                        <div id="invite" class="dropup">
                            <section class="dropdown-menu on aside-md m-l-n">
                                <section class="panel bg-white">
                                    <header class="panel-heading b-b b-light"> John <i class="fa fa-circle text-success"></i>
                                    </header> <div class="panel-body animated fadeInRight">
                                        <p class="text-sm">No contacts in your lists.</p>
                                        <p><a href="#" class="btn btn-sm btn-facebook"><i class="fa fa-fw fa-facebook"></i> Invite from Facebook</a></p>
                                    </div>
                                </section>
                            </section>
                        </div>
                        <a href="#nav" data-toggle="class:nav-xs" class="pull-right btn btn-sm btn-dark btn-icon"> <i class="fa fa-angle-left text"></i> <i class="fa fa-angle-right text-active"></i> </a>
                    </footer>
                </section>
            </aside>
            <!-- /.aside -->
            <section id="content">
                <section class="vbox">
                    <section class="scrollable padder">
                        {# breadcrumbs #}
                        {% block sonata_breadcrumb %}
                            {% if _breadcrumb is not empty or action is defined %}
                                <ul class="breadcrumb no-border no-radius b-b b-light pull-in">
                                    {% if _breadcrumb is empty %}
                                        {% if action is defined %}
                                            {% for menu in admin.breadcrumbs(action) %}
                                                {% if not loop.last  %}
                                                    <li>
                                                        {% if menu.uri is not empty %}
                                                            <a href="{{ menu.uri }}">{{ menu.label|raw }}</a>
                                                        {% else %}
                                                            {{ menu.label }}
                                                        {% endif %}
                                                    </li>
                                                {% else %}
                                                    <li class="active"><span>{{ menu.label }}</span></li>
                                                {% endif %}
                                            {% endfor %}
                                        {% endif %}
                                    {% else %}
                                        {{ _breadcrumb|raw }}
                                    {% endif %}
                                </ul>
                            {% endif %}
                        {% endblock sonata_breadcrumb %}

                        {#            <div class="m-b-md">
                                        <h3 class="m-b-none">Static Table</h3>
                                    </div>#}
                        {% block sonata_page_content %}
                            <section class="content-header">
                                {% block sonata_page_content_header %}
                                    {% block sonata_page_content_nav %}
                                        {% if _tab_menu is not empty or _actions is not empty %}
                                            <nav class="navbar navbar-default" role="navigation">
                                                {% if _navbar_title is not empty %}
                                                    <div class="navbar-header">
                                                        <span class="navbar-brand">{{ _navbar_title|raw }}</span>
                                                    </div>
                                                {% endif %}
                                                <div class="container-fluid">
                                                    <div class="navbar-left">
                                                        {% if _tab_menu is not empty %}
                                                            {{ _tab_menu|raw }}
                                                        {% endif %}
                                                    </div>

                                                    {% if _actions is not empty %}
                                                        <ul class="nav navbar-nav navbar-right">
                                                            <li class="dropdown sonata-actions">
                                                                <a href="#" class="dropdown-toggle" data-toggle="dropdown">Actions <b class="caret"></b></a>
                                                                <ul class="dropdown-menu" role="menu">
                                                                    {{ _actions|raw }}
                                                                </ul>
                                                            </li>
                                                        </ul>
                                                    {% endif %}
                                                    {# actions #}

                                                </div>
                                            </nav>
                                        {% endif %}
                                    {% endblock sonata_page_content_nav %}

                                {% endblock sonata_page_content_header %}
                            </section>

                            <section class="content">
                                {% block sonata_admin_content %}

                                    {% block notice %}
                                        {% include 'SonataCoreBundle:FlashMessage:render.html.twig' %}
                                    {% endblock notice %}

                                    {% if _preview is not empty %}
                                        <section class="panel panel-default">
                                            <header class="panel-heading">Content</header>
                                            <div class="sonata-ba-preview">{{ _preview|raw }}</div>
                                        </section>
                                    {% endif %}

                                    {% if _content is not empty %}
                                        <section class="panel panel-default">
                                            <header class="panel-heading">Content</header>
                                            <div class="sonata-ba-content">{{ _content|raw }}</div>
                                        </section>
                                    {% endif %}

                                    {% if _show is not empty %}
                                        <section class="panel panel-default">
                                            <header class="panel-heading">Content</header>
                                            <div class="sonata-ba-show">{{ _show|raw }}</div>
                                        </section>
                                    {% endif %}

                                    {% if _form is not empty %}
                                        <section class="panel panel-default">
                                            <header class="panel-heading">Content</header>
                                            <div class="sonata-ba-form">{{ _form|raw }}</div>
                                        </section>
                                    {% endif %}

                                    {% if _list_table is not empty or _list_filters is not empty %}
                                        <div class="row">
                                            <div class="sonata-ba-list {% if _list_filters|trim %}col-md-10{% else %}col-md-12{% endif %}">
                                                <section class="panel panel-default">
                                                    <header class="panel-heading">Table</header>
                                                    {{ _list_table|raw }}
                                                </section>
                                            </div>
                                            {% if _list_filters|trim %}
                                                <div class="sonata-ba-filter col-md-2">
                                                    <section class="panel panel-default">
                                                        <header class="panel-heading">Filter</header>
                                                        <div class="panel-body">
                                                            {{ _list_filters|raw }}
                                                        </div>
                                                    </section>
                                                </div>
                                            {% endif %}

                                        </div>
                                    {% endif %}
                                {% endblock sonata_admin_content %}
                            </section>
                        {% endblock sonata_page_content %}
                    </section>
                </section>
                <a href="#" class="hide nav-off-screen-block" data-toggle="class:nav-off-screen" data-target="#nav"></a>
            </section>
            <aside class="bg-light lter b-l aside-md hide" id="notes">
                <div class="wrapper">Notification</div>
            </aside>

        </section>
    </section>
</section> <!-- MOET ROND ALLES IN HET MIDDEN -->
{% endblock %}
